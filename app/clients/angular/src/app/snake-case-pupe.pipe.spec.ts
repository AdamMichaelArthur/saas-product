import { SnakeCasePupePipe } from './snake-case-pupe.pipe';

describe('SnakeCasePupePipe', () => {
  it('create an instance', () => {
    const pipe = new SnakeCasePupePipe();
    expect(pipe).toBeTruthy();
  });
});
