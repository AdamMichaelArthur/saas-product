import { Base, ResponseError } from '@base'
import { MongoClient, ObjectId } from 'mongodb';

export default class Sampledata extends Base {

	constructor(){
		super();
	}

	async search(){
		//return this.response.reply( true );
	}

}
