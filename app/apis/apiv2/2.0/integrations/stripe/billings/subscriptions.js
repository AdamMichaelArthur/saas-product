import Billings from './billings.js'

export default class Subscriptions extends Billings {

  constructor(){
    super();
  }

  updateSubscription(){
  	/*
		const subscription = await stripe.subscriptions.update(
	  'sub_1NDlt4GzpSbuU0iy4gXA5Qot',
	  {metadata: {order_id: '6735'}}
	);
  	*/
  }
}